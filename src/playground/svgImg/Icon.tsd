
import { View } from "@dlightjs/dlight";
import { div, Prop, required } from "@dlightjs/useless";

export class Icon extends View {
    @Prop _$content = required

    Body() {
        div()
            .innerHTML(this._$content)
    }
}